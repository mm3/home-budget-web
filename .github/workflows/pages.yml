name: Pages

# Publishes the app to GitHub Pages. The site is static, so the whole job is
# "check, build, upload". Enable it once under Settings -> Pages -> Source:
# GitHub Actions.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deployment at a time; a newer push waits rather than cancelling.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Unit tests and coverage
        run: npm run coverage
      - name: Build the site
        run: npm run build:site
      - name: Make sure a browser is installed
        # The browser checks drive a real Chrome over the DevTools protocol. Hosted
        # runners usually ship one; this installs one when they do not, and
        # tools/cdp.mjs finds whichever is there (CHROME_PATH overrides it).
        run: |
          BROWSER="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          if [ -z "$BROWSER" ]; then
            curl -fsSL -o /tmp/chrome.deb https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
            sudo apt-get update
            sudo apt-get install -y /tmp/chrome.deb
            BROWSER="$(command -v google-chrome)"
          fi
          echo "Using $BROWSER - $("$BROWSER" --version)"
          # Ubuntu 24.04 blocks unprivileged user namespaces, which stops Chrome's
          # sandbox from starting. The checks pass --no-sandbox, and this covers the
          # rest; the runner is thrown away after the job either way.
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 2>/dev/null || true
      - name: Check the published build
        run: npm run site-check
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
